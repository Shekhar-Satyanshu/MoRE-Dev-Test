import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MoRE App Test',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
